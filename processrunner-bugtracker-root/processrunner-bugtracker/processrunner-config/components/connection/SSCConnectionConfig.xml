<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:aop="http://www.springframework.org/schema/aop"
	xmlns:lang="http://www.springframework.org/schema/lang" xmlns:util="http://www.springframework.org/schema/util"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/aop http://www.springframework.org/schema/aop/spring-aop-4.2.xsd
		http://www.springframework.org/schema/lang http://www.springframework.org/schema/lang/spring-lang-4.2.xsd
		http://www.springframework.org/schema/util http://www.springframework.org/schema/util/spring-util-4.2.xsd">
	
	<!--==================================================================================-->
	<!-- SSC CONNECTION CONFIGURATION -->
	<!--==================================================================================-->
	
	<!-- This bean defines a proxy configuration used to access SSC. By default
	     no proxy is used. If necessary, proxy settings can either be entered here,
	     or using -SSCProxyUrl, -SSCProxyUserName and -SSCProxyPassword command line
	     options. If a proxy user name is specified without a password, the user
	     will be prompted to enter the proxy password. -->
	<bean id="sscProxy" class="com.fortify.processrunner.util.rest.ContextAwareProxyConfiguration">
		<property name="name" value="SSC"/>
		<!-- <property name="uri" value="http://proxy:port/"/> -->
		<!-- <property name="userName" value="proxy user (optional)"/> -->
		<!-- <property name="password" value="proxy password (optional)"/> -->
	</bean>
	
	<!-- Define SSC connection information like base URL, credentials, 
	     and optional proxy.
	     
	     If you require a different SSC instance, you can either change the
	     base URL here, or provide the correct SSC base URL via the
	     -SSCBaseUrl command line option (which will override the base URL 
	     configured here).
	     
	     For authentication with user credentials, user name and 
	     password can either be configured here, or provided via the 
	     command line options -SSCUserName and -SSCPassword 
	     respectively. Alternatively, if user name and/or 
	     password have not been provided either here or via command line options, 
	     the user will be prompted to enter the corresponding values.  
	     
	     For authentication with an SSC auth token, the token can either be
	     configured here, or provided via the command line 
	     option -SSCAuthToken. If not provided via either configuration or 
	     command line option, the user will be prompted for the 
	     SSC auth token to use. 
	     
	-->
	<bean id="sscConnectionRetriever" class="com.fortify.processrunner.ssc.connection.ContextAwareSSCConnectionRetrieverUserOrTokenCredentials">
		<property name="baseUrl" value="http://localhost:8080/ssc"/>
		<property name="proxy" ref="sscProxy"/>
		
		<!-- Authentication via user credentials -->
		<!-- <property name="userCreds.userName" value="username"/> -->
		<!-- <property name="userCreds.password" value="password"/> -->
		
		<!-- Authentication via token -->
		<!-- <property name="tokenCreds.authToken" value="SSC auth token"/> -->
	</bean>
</beans>
