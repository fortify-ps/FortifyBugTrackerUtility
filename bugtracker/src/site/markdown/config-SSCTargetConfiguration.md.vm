#parse("util.vm")

# Native SSC Bug Tracker Configuration

When submitting vulnerability data through a native SSC bug tracker integration, the 
configuration file must include configures SSC-related functionality. 

This configuration is defined using the following bean definition in the configuration file:

`#printTagByMatch("config/SSCToJiraViaSSC.xml" "SSCTargetConfiguration")`

This bean definition supports the following properties:

#printSSCTargetSubmitOnlyConfiguration()

Following is an example of the bean configuration for submitting vulnerabilities through the native SSC JIRA bug tracker plugin:

```xml
#printTagByMatchWithContents("config/SSCToJiraViaSSC.xml" "SSCTargetConfiguration")
```